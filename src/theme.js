import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: "#fe4",
            light: "#6b8"
        },
        secondary: {
            main: "#671",
            light: "#8b6"
        },
        otherColor: {
            main: "#1e6",
            light: "#6f8"
        },
    }
})